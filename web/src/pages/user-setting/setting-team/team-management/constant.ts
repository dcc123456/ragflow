export const RootNodeId = 'root';

export enum TriggerMemberManagementDialogType {
  Department = 'department',
  Group = 'group',
}

export enum GroupRole {
  Owner = 'owner',
  Admin = 'admin',
  Member = 'member',
}
